= form_for(current_user.active_relationships.find_by(following_id: user.id), method: :delete, remote: true) do |f|
  = hidden_field_tag :following_id, user.id
  = f.submit "フォロー中", class: "userp-following"
